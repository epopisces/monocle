{
  "name": "monocle-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "E2E test runner for Monocle (Playwright). See tests/e2e/",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:debug": "playwright test --headed --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.1"
  }
}
